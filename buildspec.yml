version: 0.2

phases:
    build:
        commands:
            - echo 'Building Reporting App............'
            - sed -i 's/^APP_ENV=.*/APP_ENV=openshift/' .env
            - zip ../cloudapp.zip -r * .[^.]*
#            - mkdir $HOME/cloudapp
#            - shopt -s dotglob
#            - rsync -rav --exclude cloudapp ./ ./cloudapp/
#            - cp -r ./ $HOME/cloudapp/
#            - zip -r cloudapp.zip .
        
artifacts:
    files: ../cloudapp.zip

