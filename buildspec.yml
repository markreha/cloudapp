version: 0.2

phases:
    build:
        commands:
            - echo 'Building Reporting App............'
            - sed -i 's/^APP_ENV=.*/APP_ENV=openshift/' .env
            - cat .env
            - mkdir cloudapp
            - bash shopt -s dotglob
            - bash rsync -rav --exclude cloudapp ./ ./cloudapp/
            - zip -r cloudapp.zip cloudapp
        
artifacts:
    files: cloudapp.zip
